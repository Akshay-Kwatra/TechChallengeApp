language: go

services: docker

before_script:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh

jobs:
  include:
    - stage: Build
      script: sh build.sh

after_script:
  - ls -lrt dist